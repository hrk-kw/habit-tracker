# 習慣トラッカー 設計メモ (ジム/ロードバイク継続用)

## コンセプト
「ジム」「自転車系」「家トレ」の3トラックを**独立して**記録・可視化する軽量トラッカー。
狙いは以下の2つ:
- サボりを可視化して心理的プレッシャーをかける(損失回避を利用)
- 記録の手間を極小化する(タップ1〜数回で完結)

**設計上の核心ルール**: 家トレはジム/自転車の代替として扱わない。
3本のカウンターを完全に独立させることで、「今日は家トレしたからジムはサボっていい」という
言い訳が構造的に成立しないようにする。

## トラック定義

| トラック | 内容 | リセット条件 |
|---|---|---|
| ジム(アウター) | ジムでのマシン筋トレ | ジムセッションを1件記録 |
| 自転車系 | ロードバイク実走 or ジムの固定バイク | どちらかを1件記録(同一カウンターに合算) |
| 家トレ(インナー) | 自重・体幹・ストレッチ | 家トレを1件記録(他トラックに影響しない) |

## 可視化ロジック
ホーム画面に3枚のカードを表示。各カードに「前回から◯日」を大きく表示し、
経過日数に応じて色を変える(トラックごとに閾値は別設定可能。例: 緑→黄→赤)。

## データモデル

```
Exercise {
  id, name,
  category: "outer" | "inner",   // outer=ジムのマシン系, inner=家トレ系
  default_weight_kg,             // 前回記録値を引き継ぐ。初回はnull
  default_reps
}

SetLog {
  id, exercise_id, date, weight_kg, reps, set_index, timestamp
}

Session {
  id, date,
  type: "gym" | "bike" | "home",
  bike_mode?: "outdoor" | "gym_machine",  // type=bikeの時のみ
  duration_min?, note?
  // avg_hr / max_hr は v1.1でHR連携時に追加
}
```

「経過日数」はSessionのtype別最新dateから都度計算する派生値。DBには保持しない。

## 画面フロー

### ホーム画面
3トラックのカード(経過日数+色分け)。タップで各記録画面へ。

### ジム記録画面
固定メニュー一覧。各行に「前回: 60kg×10回」のように前回値を参考表示。
- タップ = そのExerciseに1セット追加(前回の重量/回数をそのまま使用)
- 3タップ = 3セット完了として記録
- 重量/回数を変えたい時だけ長押しで編集
- マシンが埋まっていて出来なかった種目は0セットのまま放置でOK(個別種目の未達に罰則なし。
  罰則があるのは「今日ジムに行ったかどうか」というセッション単位のみ)
- 保存ボタンでSession(type=gym)を1件確定 → ジムの経過日数リセット

### 自転車記録画面
「実走」か「ジムの固定バイク」を選択+時間(任意)。距離・ペースは既にStravaで管理しているので
二重管理しない。保存でSession(type=bike)確定。

### 家トレ記録画面
自重メニューの簡易チェックリスト。1つでも完了したらSession(type=home)確定。

## 固定メニュー初期データ

```
outer (ジム):
  レッグプレス
  チェストプレス
  ラットプルダウン
  シーテッドロー
  レッグカール
  アブドミナルクランチ
  ロータリートルソー
  [任意追加候補] バックエクステンション   // 腰背部の伸展。前傾姿勢の腰痛予防に有効
  [任意追加候補] ショルダープレス         // チェストプレスだけだと肩の押しが手薄

inner (家):
  グルートブリッジ
  デッドバグ
  プランク
  膝抱え込みストレッチ(仰向け、片膝を胸に引き寄せ)
  [任意追加候補] クラムシェル             // 中臀筋。ペダリング中の膝の内倒れ(ニーイン)予防
  [任意追加候補] 胸を開くストレッチ       // 前傾姿勢で固まる胸まわりのケア
```

追加候補species(バックエクステンション/ショルダープレス/クラムシェル/胸開きストレッチ)は
採用するか後で決めてよい。単にExerciseレコードを増やすだけで組み込める。

## 可視化・通知(ネイティブウィジェット不要案)

PWAのままではAndroidのホーム画面ウィジェット(AppWidget)は作れない(ネイティブAndroid専用API)。
そこでMVPでは以下3点の組み合わせで代替する:

1. **常駐通知(ongoing notification)**: 通知シェードに「ジム: 3日経過 / 自転車: 12日経過」を
   常時表示(消えにくい設定)
2. **通知アクションボタン**: 通知を開かずに、ボタンタップだけで「ジム行った」「自転車乗った」を
   記録できるようにする(Web Notifications APIの`actions`で実装)
3. **アイコンバッジ(Badging API)**: ホーム画面アイコンに経過日数を数字表示
   (インストール済みPWA=WebAPK化していればAndroid Chromeで動作)

将来的に「やっぱりホーム画面に物理的なウィジェットが欲しい」となった場合は、
Trusted Web Activity(TWA)でネイティブアプリ化し、Kotlinで別途AppWidgetProviderを
書いて共有ストレージを読ませる形になる。ただし実装コストが大きく上がるため、
まずPWA+常駐通知/バッジで運用してから判断する。

## HR(心拍)連携について

CYCPLUS H1に表示機器・純正アプリでの確認手段がないため、手入力によるMVP運用は不可。
ただしStravaが既にH1と直接Bluetoothペアリングして読めている実績があり、
これは「H1が標準BLE Heart Rate Service(0x180D)をブロードキャストしている」ことの
実証になっている。つまりWeb Bluetooth APIでの直接連携は技術的な不確実性が低い。

**判断**: MVPでは見送り。まず習慣トラッカー自体が続くかを検証してから、
v1.1でジム記録画面にWeb Bluetooth経由のリアルタイムHR取得(ペアリング→
セッション中ストリーム受信→平均/最大を自動計算)を追加する。
ロードバイクは引き続きStravaで計測(変更なし)。

## 技術スタック

- PWA(manifest.json + service worker)、ホーム画面に追加してネイティブアプリ的に起動
- データはIndexedDBに端末内完結で保存(バックエンド不要、1人利用前提)
- 複数端末同期が必要になったら後から軽量BaaS(Supabase等)を追加検討(MVPでは不要)

## スコープまとめ

**MVP**
- 3トラックのホーム画面(経過日数+色分け)
- ジム記録(セットログ、前回値引き継ぎ、追加種目候補込み)
- 自転車/家トレの記録(ワンタップ)
- 常駐通知(経過日数表示+ワンタップ記録アクション)+ アイコンバッジ

**v1.1以降(継続確認後に投資判断)**
- HR自動記録(Web Bluetooth、H1連携)
- ネイティブウィジェット化(TWA化を検討)
